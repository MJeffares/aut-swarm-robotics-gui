<Window x:Class="SwarmRoboticsGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SwarmRoboticsGUI"
        
        
        xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        xmlns:em="clr-namespace:Emgu.CV.UI;assembly=Emgu.CV.UI"
        
        mc:Ignorable="d"
        Title="Swarm Robotics Graphics User Interface" Height="350" Width="525" >
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="Column1" Width="*"/>
            <ColumnDefinition x:Name="Column2" Width="5"/>
            <ColumnDefinition x:Name="Column3" Width="*"/>
        </Grid.ColumnDefinitions>


        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"/>


        <DockPanel Grid.Column="2">
            
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="_File">
                    <MenuItem Header="_New" />
                    <MenuItem Header="_Open" />
                    <MenuItem Header="_Save" />
                </MenuItem>
                <MenuItem Header="Camera" MouseEnter="MenuItem_MouseEnter">
                    <MenuItem x:Name="menuCameraList" Header="Connected Cameras" MouseEnter="menuCameraList_MouseEnter"/>
                    <MenuItem x:Name="menuCameraConnect" Header="Start Capture" IsEnabled="False" IsCheckable="True" Checked="menuCameraConnect_Checked"/>
                </MenuItem>
                <MenuItem Header="Filters">
                    
                </MenuItem>
                <MenuItem Header="Overlays">
                    
                </MenuItem>
                <MenuItem Header="Options"/>
            </Menu>

            <WindowsFormsHost Name="host1" HorizontalAlignment="Stretch" Height="Auto" VerticalAlignment="Stretch" Width="Auto" Margin="0,0,0,0" Visibility="Hidden">
                <em:ImageBox x:Name="captureImageBox" Height="320" Width="256" SizeMode="StretchImage" FunctionalMode="Minimum"/>
            </WindowsFormsHost>
        </DockPanel>
    </Grid>
</Window>
